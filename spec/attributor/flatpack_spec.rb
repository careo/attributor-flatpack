require 'spec_helper'

describe Attributor::Flatpack do
  it 'has a version number' do
    expect(Attributor::Flatpack::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
